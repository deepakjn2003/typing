# gemini API Key (for summarization)
GEMINI_API_KEY=your_gemini_api_key_here

# gemini llm model selection (change if needed)
# TYPING_GEMINI_MODEL=gemini-2.5-flash

# sqlite db path (default: ~/.typing/typing.db)
# TYPING_DB_PATH=
